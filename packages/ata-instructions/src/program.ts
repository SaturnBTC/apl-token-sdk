import { Pubkey } from '@saturnbtcio/arch-sdk';

// Default Associated Token Account program id for Arch
export const ASSOCIATED_TOKEN_PROGRAM_ID: Pubkey = new Uint8Array(
  Buffer.from('AssociatedToken111111111111111'),
) as unknown as Pubkey;


